---
"@wisemen/vue-core-components": patch
---

Add `isPublic` prop to FileUpload component to allow users to specify if the uploaded file should be public or private. 
This prop will be used to set the appropriate permissions when uploading the file to the server.
