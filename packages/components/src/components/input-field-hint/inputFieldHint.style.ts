import { tv } from '@/libs/twVariants.lib'

export const inputFieldHintStyle = tv({
  slots: {
    hint: 'mt-input-field-hint-spacing-default block text-input-field-hint-font-size-default text-input-field-hint-text-color-default duration-200',
  },
})
