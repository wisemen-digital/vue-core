import { tv } from '@/libs/twVariants.lib'

export const keyboardStyle = tv({
  slots: {
    key: 'flex h-[1.1rem] min-w-[1.1rem] items-center justify-center rounded border border-solid p-1 text-center font-sans text-[0.6875rem] capitalize',
  },
})
