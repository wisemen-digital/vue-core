import { tv } from '@/libs/twVariants.lib'

export const keyboardStyle = tv({
  slots: {
    key: 'flex h-[1.2rem] min-w-[1.2rem] items-center justify-center rounded border border-solid border-primary bg-secondary py-1 text-center text-caption capitalize text-tertiary',
  },
})
