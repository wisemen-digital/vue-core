import type { Component } from 'vue'
import type { RouteRecordRaw } from 'vue-router'

export const <%= camelCase %>Routes = [
  {
    path: '/users',
    children: [

        name: '<%= kebabCase %>-overview',
        path: '',
        component: (): Component => import('@/modules/<%= kebabCase %>/features/overview/views/<%= pascalCase %>OverviewView.vue'),
      },
      {
        name: '<%= kebabCase %>-detail',
        props: true,
        path: ':<%= camelCase %>Uuid',
        component: (): Component => import('@/modules/<%= kebabCase %>/features/detail/views/<%= pascalCase %>DetailViewDataProvider.vue'),
      },
      {
        name: '<%= kebabCase %>-update',
        props: true,
        path: ':<%= camelCase %>Uuid/update',
        component: (): Component => import('@/modules/<%= kebabCase %>/features/update/views/<%= pascalCase %>UpdateViewDataProvider.vue'),
      },
    ],
  },
] as const satisfies RouteRecordRaw[]
