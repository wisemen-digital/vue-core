# Interactable

A wrapper component that provides consistent styling and behavior for interactive elements. It applies focus, disabled, and cursor states with keyboard accessibility support.

<ComponentPreview name="interactable/examples/main" />

## Props

- `class` - Additional CSS classes to apply to the element
- `as` - The HTML element or Vue component to render as (default: `div`)
