{
  "name": "@wisemen/vue-core",
  "type": "module",
  "version": "0.0.0",
  "packageManager": "pnpm@8.15.4",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "rimraf packages/vue-core/dist && pnpm run -r build",
    "pub:release": "cd packages/components && pnpm pub:release"
  },
  "devDependencies": {
    "@wisemen/eslint-config-vue": "^0.0.6",
    "eslint": "^8.57.0",
    "rimraf": "^5.0.5"
  }
}
