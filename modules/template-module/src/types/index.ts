/**
 * Module options passed during setup
 * Customize these based on your module's requirements
 */
export interface TemplateModuleOptions {
  /** Example option - replace with your needs */
  apiUrl?: string

  /** Example option - replace with your needs */
  config?: Record<string, unknown>
}
