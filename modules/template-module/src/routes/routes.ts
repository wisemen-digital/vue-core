import type { RouteRecordRaw } from 'vue-router'

/**
 * Routes exported by the Template module
 * Import these in your main router setup:
 *
 * @example
 * ```typescript
 * import { templateModuleRoutes } from '@wisemen/template-module/routes'
 *
 * const router = createRouter({
 *   routes: [
 *     ...templateModuleRoutes,
 *   ],
 * })
 * ```
 */
export const templateModuleRoutes: RouteRecordRaw[] = [
  // TODO: Add your module routes here
  // {
  //   path: '/template',
  //   component: () => import('./pages/TemplatePage.vue'),
  //   children: [/* ... */],
  // },
]
